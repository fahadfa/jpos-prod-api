/auth/forgotpassword:
    get:
        summary: Forgot password sent otp
        tags:
            - Access
        parameters:
            - name: userName
              example: "admin@abcd.com"
              in: query
              required: true
              type: string

        responses:
            200:
                description: A single user item
