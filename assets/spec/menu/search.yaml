/menu:
  get:
    tags:
      - Menu
    description:
      search.
    parameters:
      - name: name
        in: query
        required: false
        type: string
    responses:
        200:
            description: "status:1 is success; status: 0 is failure;"