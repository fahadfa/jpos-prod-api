/salestarget/search/top20:
  get:
    tags:
      - salestargets
    description: search.
    parameters:
      - name: unittime
        in: query
        required: true
        type: string
        example: "DAY"
      - name: inventlocationid
        in: query
        required: true
        type: string
        example: "RMAW-0010"
    responses:
      200:
        description: "status:1 is success; status: 0 is failure;"
