/syncsource:
  get:
    tags:
      - Sync Source
    description: search.
    parameters:
      - name: id
        in: query
        required: false
    responses:
      200:
        description: "status:1 is success; status: 0 is failure;"
